version: 1.0.0
description: My awesome orb
commands:
  my-command:
    parameters:
      message:
        type: string
        default: "Hello, world!"
    steps:
      - run: echo "${{ orb.parameters.message }}"

